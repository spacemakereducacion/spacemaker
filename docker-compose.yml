services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: spacemaker
      POSTGRES_PASSWORD: spacemaker_dev
      POSTGRES_DB: spacemaker
    ports:
      - "5432:5432"
    volumes:
      - spacemaker_pg:/var/lib/postgresql/data

volumes:
  spacemaker_pg:
